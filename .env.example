# Set this in the Convex dashboard, not in Vercel.
AUDIT_PASSCODE=choose-a-strong-shared-passcode

# Convex writes this during local setup. Vercel receives it during `convex deploy`.
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# Add a production or preview deploy key in Vercel.
CONVEX_DEPLOY_KEY=
